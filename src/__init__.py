# src/__init__.py
#
# Makes `src` a Python package so its modules can be imported with:
#   from src.data_loader import load_raw_data
#   from src.model import DemandModel
#
# Re-export top-level symbols here as the package grows.
